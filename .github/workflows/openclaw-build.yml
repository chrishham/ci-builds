name: "OpenClaw: Build & Deploy"

on:
  schedule:
    # Every Thursday at 06:00 UTC (09:00 Athens time)
    - cron: '0 6 * * 4'
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch of chrishham/openclaw to build from (empty = retag upstream)'
        required: false
        default: ''
      deploy:
        description: 'Deploy to VPS after build'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

env:
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/chrishham/openclaw

jobs:
  # --- Default: retag upstream's official image ---
  retag-upstream:
    if: github.event.inputs.branch == '' || github.event.inputs.branch == 'main'
    runs-on: ubuntu-24.04
    permissions:
      packages: write
    outputs:
      tag: ${{ steps.tag.outputs.tag }}

    steps:
      - name: Sync fork with upstream
        run: gh repo sync chrishham/openclaw --branch main --source openclaw/openclaw
        env:
          GH_TOKEN: ${{ secrets.FORK_PAT }}

      - name: Determine tag
        id: tag
        run: |
          VERSION=$(curl -s https://raw.githubusercontent.com/openclaw/openclaw/main/package.json | jq -r '.version')
          TIMESTAMP=$(date -u '+%Y%m%d-%H%M')
          TAG="${VERSION}-${TIMESTAMP}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "Retagging upstream ${VERSION} as ${TAG}"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Retag upstream image
        run: |
          docker buildx imagetools create \
            -t ${{ env.IMAGE }}:${{ steps.tag.outputs.tag }} \
            -t ${{ env.IMAGE }}:latest \
            ghcr.io/openclaw/openclaw:latest

  # --- Custom branch: compute tag once, then build per-arch ---
  prepare-custom:
    if: github.event.inputs.branch != '' && github.event.inputs.branch != 'main'
    runs-on: ubuntu-24.04
    outputs:
      tag: ${{ steps.tag.outputs.tag }}

    steps:
      - name: Clone and compute tag
        id: tag
        run: |
          git clone --depth 1 --branch "${{ github.event.inputs.branch }}" https://github.com/chrishham/openclaw.git src
          cd src
          VERSION=$(jq -r '.version' package.json)
          SHORT_SHA=$(git rev-parse --short HEAD)
          TIMESTAMP=$(date -u '+%Y%m%d-%H%M')
          TAG="${VERSION}-${TIMESTAMP}-${SHORT_SHA}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "Building ${TAG} from branch ${{ github.event.inputs.branch }}"

  build-amd64:
    needs: prepare-custom
    runs-on: ubuntu-24.04
    permissions:
      packages: write

    steps:
      - name: Clone openclaw fork
        run: git clone --depth 1 --branch "${{ github.event.inputs.branch }}" https://github.com/chrishham/openclaw.git src

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push amd64
        uses: docker/build-push-action@v6
        with:
          context: ./src
          file: ./src/Dockerfile
          platforms: linux/amd64
          push: true
          provenance: false
          tags: ${{ env.IMAGE }}:${{ needs.prepare-custom.outputs.tag }}-amd64

  build-arm64:
    needs: prepare-custom
    runs-on: ubuntu-24.04-arm
    permissions:
      packages: write

    steps:
      - name: Clone openclaw fork
        run: git clone --depth 1 --branch "${{ github.event.inputs.branch }}" https://github.com/chrishham/openclaw.git src

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push arm64
        uses: docker/build-push-action@v6
        with:
          context: ./src
          file: ./src/Dockerfile
          platforms: linux/arm64
          push: true
          provenance: false
          tags: ${{ env.IMAGE }}:${{ needs.prepare-custom.outputs.tag }}-arm64

  create-manifest:
    needs: [prepare-custom, build-amd64, build-arm64]
    runs-on: ubuntu-24.04
    permissions:
      packages: write
    outputs:
      tag: ${{ needs.prepare-custom.outputs.tag }}

    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create multi-arch manifest
        run: |
          TAG="${{ needs.prepare-custom.outputs.tag }}"
          docker buildx imagetools create \
            -t ${{ env.IMAGE }}:${TAG} \
            ${{ env.IMAGE }}:${TAG}-amd64 \
            ${{ env.IMAGE }}:${TAG}-arm64

  # --- Cleanup old images (keep last 30) ---
  cleanup:
    needs: [retag-upstream, create-manifest]
    if: always() && (needs.retag-upstream.result == 'success' || needs.create-manifest.result == 'success')
    runs-on: ubuntu-24.04
    permissions:
      packages: write

    steps:
      - name: Delete old image versions
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSIONS=$(gh api --paginate /user/packages/container/openclaw/versions \
            --jq 'sort_by(.created_at) | reverse | .[].id')
          COUNT=0
          DELETED=0
          for ID in $VERSIONS; do
            COUNT=$((COUNT + 1))
            if [ "$COUNT" -le 30 ]; then
              continue
            fi
            echo "Deleting version ID: $ID"
            gh api -X DELETE /user/packages/container/openclaw/versions/$ID 2>/dev/null || true
            DELETED=$((DELETED + 1))
          done
          echo "Kept $((COUNT < 30 ? COUNT : 30)) versions, deleted ${DELETED}"

  # --- Deploy ---
  deploy:
    needs: [retag-upstream, create-manifest]
    if: |
      always() &&
      (github.event.inputs.deploy != 'false') &&
      (needs.retag-upstream.result == 'success' || needs.create-manifest.result == 'success')
    runs-on: ubuntu-24.04

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ubuntu
          key: ${{ secrets.OCI_SSH_KEY }}
          script: |
            TAG="${{ needs.retag-upstream.outputs.tag || needs.create-manifest.outputs.tag }}"
            kubectl set image deployment/openclaw openclaw=ghcr.io/chrishham/openclaw:${TAG}
            kubectl rollout status deployment/openclaw --timeout=120s
